# gmail skill dependencies
#   pip install -r requirements.txt
google-api-python-client>=2.100
google-auth>=2.23
google-auth-httplib2>=0.1
# Only needed to run authorize.py (the one-time consent bootstrap):
google-auth-oauthlib>=1.1
