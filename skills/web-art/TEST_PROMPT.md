# Web Art Skill — Test Prompt

Copy and paste the prompt below into a conversation to test the skill end-to-end.

---

Create a simple test page at `test-web-art/index.html` with a dark navy background (`#1a1a2e`) and generate the following 5 transparent assets using the web-art skill:

**Icons (1K resolution):**
1. A rocket icon
2. A compass icon
3. A lightning bolt icon

**Cover images (2K resolution):**
4. A horizontal landscape scene (mountains with a sunset)
5. A vertical portrait illustration (a tall tree)

Display all 5 assets on the test page — icons in a row of cards, covers below in a side-by-side layout. The transparent backgrounds should let the navy page color show through cleanly with no magenta fringe or border artifacts.

After generating, open the page in the browser so I can inspect the results. Keep both the raw (with chroma-key background) and final (transparent) files.
